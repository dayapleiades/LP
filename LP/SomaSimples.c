#include <stdio.h>  // Inclusão de biblioteca
#include <math.h> 

int main() // Programa principal
{
    int A, B, SOMA; // Declação de variáveis
    scanf ("%d", &A); // Leitura de variáveis
    scanf ("%d", &B); // Leitura de variáveis
    SOMA=A+B;  // Cálculo de soma
    printf("SOMA = %d\n",SOMA); // Mostra o Resultado
    return 0;
}
