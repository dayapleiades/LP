#include <stdio.h> // Inclusão de bibliotecas
 
int main() { // Programa principal
 
    int A, B, X; // Declaração de variáveis
    scanf ("%d", &A); // Leitura de variáveis 
    scanf ("%d", &B); // Leitura de variáveis
    X = A + B; // Soma de duas variáveis
    printf("X = %d\n", X); // Mostra o Resultado
    return 0;
}
