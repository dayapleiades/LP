#include <stdio.h> // Inclusão de bibliotecas
#include <math.h>

int main() // Programa principal
{
     int A, B, C, D, DIFERENCA; // Declaração de variáveis
     scanf ("%d %d %d %d", &A, &B, &C, &D); // Leitura de variáveis
     DIFERENCA = (A*B-C*D); // Cálculo da diferença
            printf("DIFERENCA = %d\n",DIFERENCA); // Mostra a diferença
    return 0;
}
